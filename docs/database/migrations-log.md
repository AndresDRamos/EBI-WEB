# Migrations log

Chronological record of applied Flyway migrations. `/ship-module` or `/plan-module`
registers each plan's migrations here on approval and applies them to `EBI_dev`; the
`docs-sync` sub-agent reconciles after a successful `flyway migrate`.

| Version | File | Description | Applied (env / date) |
|---|---|---|---|
| V1 | `db/migrations/V1__init.sql` | Report metadata (`dbo.report`, `dbo.report_category`) | EBI_dev / 2026-06-24 |
| V2 | `db/migrations/V2__schemas_staging_core.sql` | `staging` + `core` + `etl` schemas and `etl.run_log` | EBI_dev / 2026-06-24 |
| V3 | `db/migrations/V3__auth_schema.sql` | Portal-owned auth RBAC (`auth` schema: `app_user`, `role`, `plant`, `department`, `user_role`, `user_plant`, `user_department`, `invitation`); seeds `admin` and `viewer` roles | EBI_dev / 2026-06-26 |
| V4 | `db/migrations/V4__user_admin_catalog_columns.sql` | User-admin catalog columns (`auth.role.is_active`; `auth.department.description`; `auth.plant.address`, `auth.plant.postal_code`) | EBI_dev / 2026-06-27 |
| V5 | `db/migrations/V5__maint_asset_catalog.sql` | Mantenimiento part 1: `maint` schema + asset catalog (`process`, `asset`, `asset_process`, `asset_restriction`, `asset_document`) — plan 0004 | EBI_dev / 2026-07-01 (verified via `ebi-sql-dev` `flyway_schema_history`) |
| V6 | `db/migrations/V6__maint_plans_workorders_spares.sql` | Mantenimiento part 2: `spare_part`, `maintenance_plan`, `plan_task`, `plan_material`, `work_order`, `work_order_task`, `work_order_material`, `stock_movement` — plan 0004 | EBI_dev / 2026-07-01 (verified via `ebi-sql-dev` `flyway_schema_history`) |
| V7 | `db/migrations/V7__nav_registry.sql` | Portal layout & navigation: `auth.nav_section`, `auth.nav_item`, `auth.role_nav_section`; seeds `dashboards` (active) and `maintenance` (inactive) sections — plan 0005 | EBI_dev / 2026-07-02 (verified via `ebi-sql-dev` `flyway_schema_history`) |
| V8 | `db/migrations/V8__rbac_permissions.sql` | Resource+action RBAC: `auth.role.department_id` (access-profile semantics), `auth.permission` (35 seeded codes `<module>.<resource>:<action>`), `auth.role_permission` (ships empty) — plan 0006 | EBI_dev / 2026-07-02 (verified via `ebi-sql-dev` `flyway_schema_history`) |
| V9 | `db/migrations/V9__maintenance_nav_items.sql` | Maintenance nav items (blueprint retrofit): idempotent seed of `auth.nav_item` rows `Máquinas → /maintenance/machines` and `Procesos → /maintenance/process`; data-only, no DDL — plan portal-home-nav-authz | EBI_dev / 2026-07-03 (user-confirmed `flyway migrate` + `pnpm db:gen`; not independently verified via `flyway_schema_history` — `ebi-sql-dev` MCP offline this session) |
| V10 | `db/migrations/V10__drop_reports_powerbi.sql` | Power BI purge cleanup: DROP `dbo.report` + `dbo.report_category` (both empty; `dbo` left with no portal tables) and DELETE the 6 inert `auth.permission` `reports.%` codes (0 grants) — plan portal-home-nav-authz. **Irreversible** (DROP TABLE). Run `pnpm db:gen` after apply | EBI_dev / 2026-07-03 (row was stale at `applied: false`; reconciled 2026-07-03 via this session's `flyway info`: installed 18:00:33, Success) |
| V11 | `db/migrations/V11__produccion_schema.sql` | `produccion` schema (`production_line`, `cell` with nullable line + filtered-unique op sequence, temporal `asset_cell_assignment` with filtered-unique current row); `maint.asset.asset_category` + `IX_asset_category`; seeds nav section `production` (dark-launch) + 2 items + 6 `production.*` permissions — plan production-cell-assignment. All additive, nothing irreversible | EBI_dev / 2026-07-03 (verified via `flyway info`: Schema version 11, Success; `pnpm db:gen` regenerated 32 tables) |
| V12 | `db/migrations/V12__rename_produccion_schema_to_production.sql` | Rename schema `produccion` → `production` (English DB naming): `CREATE SCHEMA production` + guarded `ALTER SCHEMA TRANSFER` of the 3 tables (metadata-only; FKs/indexes/statistics intact) + re-issued schema grants (`ebi_app`, `ebi_agent_ro`) + `DROP SCHEMA produccion`. **No clean undo** (drop of the old schema); no data loss — plan production-schema-rename | EBI_dev / 2026-07-06 (verified via `flyway info`: Schema version 12, Success; `pnpm db:gen` regenerated 32 tables) |
| V13 | `db/migrations/V13__production_plant_layout.sql` | Plant layouts (plan plant-layout-foundation): `production.plant_layout` (versioned immutable canvas, one active per plant via filtered unique, ISJSON geometry, lifecycle timestamps), `production.asset_footprint` (one per asset, `dxf`\|`rectangle`), `production.asset_placement` (temporal close+insert, filtered-unique current per layout+asset); nav item `Layout` + 6 `production.*` permission codes. All additive, nothing irreversible; no new grants (V12 schema grants cover) | EBI_dev / 2026-07-06 (verified via `flyway info`: Schema version 13, Success; `pnpm db:gen` regenerated 35 tables; re-verified post-build via `ebi-sql-dev` `flyway_schema_history`: version 13, success) |
| V14 | `db/migrations/V14__remove_plant_layout_nav_item.sql` | Removes the V13-seeded `Layout` nav item (plan plant-layout-foundation, re-scope amendment 2026-07-06): the module dark-parks under the admin-only `/test/layout` area, outside the nav registry. DML only (single seed-row DELETE, reversible by re-running the V13 seed); tables, permissions and grants untouched | EBI_dev / 2026-07-06 (verified via this session's `flyway migrate`: now at v14; nav-item count for `/production/layout` = 0 via `ebi-sql-dev`) |
| V15 | `db/migrations/V15__org_schema_plant_process.sql` | New `org` schema (plan org-schema-plant-process): `CREATE SCHEMA org` + guarded `ALTER SCHEMA TRANSFER` of `auth.plant`→`org.plant` and `maint.process`→`org.process` (metadata-only; FKs re-point by object_id, indexes/stats intact) + new N:M `org.plant_process` (link-row, `IX_plant_process_process`) + seed `org.process:{create,update,delete}` / `org.plant_process:assign`, retire `maintenance.process:*` (cascades grants) + guarded DELETE of the maintenance `Procesos` nav item + `org` schema grants (`ebi_app` CRUD, `ebi_agent_ro` SELECT). **Irreversible** (schema transfers; coordinated code cutover to `.withSchema("org")`) | EBI_dev / 2026-07-07 (verified via this session's `flyway migrate`: now at v15, Success; `flyway info` clean; `pnpm db:gen` regenerated 36 tables) |
| V16 | `db/migrations/V16__role_nav_item.sql` | Page-granular nav authz (plan admin-permissions-portal): new `auth.role_nav_item (role_id, item_id, priority)` — per-role page visibility + intra-section order — PK `(role_id, item_id)`, FK `role` NO ACTION / FK `nav_item` ON DELETE CASCADE, `IX_role_nav_item_item`; idempotent backfill fans out each `role_nav_section` grant to every active `nav_item` of its section (`priority = sort_order`). `role_nav_section` kept (semantics narrow to section order). Additive + data backfill — **reversible at DB level** (`DROP TABLE` restores prior state); irreversibility is the coupled app cutover. Supersedes ADR 0005 → ADR 0008 | EBI_dev / 2026-07-08 (verified via this session's `flyway migrate`: now at v16, Success; `flyway info` clean; `pnpm db:gen` regenerated 37 tables; backfill pre-check via `ebi-sql-dev`: 0 role_nav_section rows point at a section with no active items) |
