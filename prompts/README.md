# prompts/

Created on-demand by the full lane: `/plan-module` writes `prompts/<slug>.md`
as the ready-to-run brief for whichever agent runs `/build-plan`. Empty
between plans — do not pre-populate it.
